# SimuTrader

Monorepo for the SimuTrader web app (Next.js + FastAPI + Postgres + Redis + Celery + DuckDB/Parquet).

## Quick start

1. Copy env templates as needed.
2. Run `make up`.
3. API health: `http://localhost:8000/health`.
